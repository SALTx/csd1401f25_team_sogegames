#include "cprocessing.h"
#include "../abstraction.h"


int tileMap[] = {
	0, 0, 0,
	0, 0, 0,
	0, 0, 0
};

void level1(void) {
	// Not implemented yet 
	fill(WHITE);
	textSize(32.0f);
	text("Level 1: Not implemented yet", WIDTH / 2, HEIGHT / 2);
	text("Press Q to go back", WIDTH / 2, HEIGHT / 2 + 16.0f);
}
