#include "cprocessing.h"
#include "../abstraction.h"

void level3(void) {
	// Not implemented yet 
	fill(WHITE);
	textSize(32.0f);
	text("Level 3: Not implemented yet", WIDTH / 2, HEIGHT / 2);
	text("Press Q to go back", WIDTH / 2, HEIGHT / 2 + 16.0f);
}
